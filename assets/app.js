let DATA=null;const $=s=>document.querySelector(s);async function init(){DATA=await fetch("data/results.json").then(r=>r.json());renderStats();renderTasks();renderModels();populateTasks();updateBenchmarks();renderResults();$("#taskSelect").onchange=()=>{updateBenchmarks();renderResults()};$("#benchmarkSelect").onchange=renderResults;$("#trackSelect").onchange=renderResults}function renderStats(){$("#taskCount").textContent=DATA.tasks.length;$("#benchmarkCount").textContent=DATA.benchmarks.length;$("#modelCount").textContent=DATA.models.length;$("#resultCount").textContent=DATA.results.length}function renderTasks(){$("#taskCards").innerHTML=DATA.tasks.map((t,i)=>`<article class="task-card ${t.status==="active"?"active":""}"><div class="num">${String(i+1).padStart(2,"0")}</div><div><span class="tag ${t.status==="active"?"active":""}">${t.status.toUpperCase()}</span><h3>${t.name}</h3><p>${t.description}</p>${t.status==="active"?`<a href="#leaderboard" onclick="setTask('${t.id}')">View performance →</a>`:""}</div></article>`).join("")}function renderModels(){$("#modelGrid").innerHTML=DATA.models.length?DATA.models.map(m=>`<article class="model-card"><span class="tag">${m.type||"ENCODER"}</span><h3>${m.name}</h3><p>${m.description||""}</p><a href="${m.hf_url}" target="_blank" rel="noopener">Hugging Face ↗</a></article>`).join(""):`<article class="model-card"><h3>No models yet</h3><p>Verified candidate models will be added with their Hugging Face links.</p></article>`}function populateTasks(){$("#taskSelect").innerHTML=DATA.tasks.map(t=>`<option value="${t.id}">${t.name}</option>`).join("")}function updateBenchmarks(){let bs=DATA.benchmarks.filter(b=>b.task===$("#taskSelect").value);$("#benchmarkSelect").innerHTML=bs.length?bs.map(b=>`<option value="${b.id}">${b.name}</option>`).join(""):`<option value="">No benchmarks yet</option>`}function renderResults(){let task=$("#taskSelect").value,benchmark=$("#benchmarkSelect").value,track=$("#trackSelect").value;let rows=DATA.results.filter(r=>r.task===task&&r.benchmark===benchmark&&(track==="all"||r.track===track)).sort((a,b)=>b.score-a.score);$("#resultsBody").innerHTML=rows.map((r,i)=>{let m=DATA.models.find(x=>x.id===r.model);return `<tr><td>${i+1}</td><td><a class="model-link" href="${m?.hf_url||"#"}" target="_blank">${m?.name||r.model}</a></td><td>${r.track_label}</td><td class="score">${r.score.toFixed(2)}</td><td>${r.metric}</td></tr>`}).join("");$("#noResults").classList.toggle("hidden",rows.length>0)}function setTask(id){$("#taskSelect").value=id;updateBenchmarks();renderResults()}init();